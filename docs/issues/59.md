# Issue #59 — Refresh README screenshots for final reader UI

## Problem

README screenshots predated the M6 reader enhancements: spread mode, cover-alone view, five flip presets, thumbnail scrubber, 20+ generated pages per book, and sharp-corner default. The reader shots also cropped the bottom controls because they used viewport-only captures.

## Approach

1. Updated `scripts/screenshots.ts`:
   - Seeds `viewMode=spread` and `coverMode=single` for reader shots.
   - Navigates to the first interior spread (pages 2–3) so two-page layout, scrubber, and flip-style picker are visible.
   - Uses `fullPage: true` to include the full reader chrome.
2. Regenerated all four PNGs in `docs/media/`.
3. Refreshed README **Features** and **Screenshots** sections; deduplicated the Scripts table.

## Verification

- [x] `pnpm screenshots` — all four images captured
- [x] Visual review — spread reader with scrubber + flip presets visible
- [x] `pnpm lint` — clean (docs-only + script change; Vitest / Playwright skipped)
